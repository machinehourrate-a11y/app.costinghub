import React from 'react';

// This component is intentionally left empty and returns null.
// All subscription plan management is handled externally as per the system architecture.
// This file is kept to prevent import errors in other parts of the code during refactoring.
export const PlanModal: React.FC = () => {
  return null;
};
